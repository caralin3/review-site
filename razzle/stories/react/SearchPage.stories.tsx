import React from 'react';
import { storiesOf } from '@storybook/react';
import { SearchPage } from '../../src/client/react/pages';

storiesOf('Pages|Search', module).add('content', () => <SearchPage />);
