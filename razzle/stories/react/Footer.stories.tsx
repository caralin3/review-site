import React from 'react';
import { storiesOf } from '@storybook/react';
import { Footer } from '../../src/client/react/components';

storiesOf('Components|Footer', module).add('with text', () => <Footer />);
